# Dashboard screenshots

This directory contains real captures from the existing DataDrift Monitor dashboard used by the landing page.

Current screenshots:

- `dashboard-summary.png`
- `severity-investigation.png`
- `feature-analysis.png`

They are presented as semantic `<figure>` and `<img>` elements in `../index.html`, with alt text identifying the dashboard view shown.
